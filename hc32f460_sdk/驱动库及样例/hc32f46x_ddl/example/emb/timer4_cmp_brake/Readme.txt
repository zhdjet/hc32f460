================================================================================
                                样例使用说明
================================================================================
版本历史
日期        版本    负责人         IAR     MDK     GCC     描述
2018-12-04   1.0     Husj           7.70    5.16    8.3.1   first version
================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。


功能描述
================================================================================
本样例主要展示MCU的EMB模块的比较刹车功能。
说明：
EMB2用于Timer4，CMP1 INP使用PA00，INM使用PC03或者DAC1
（EMB2-4用于Timer4）
 EMB2中断内设置软件刹车，清除CMP刹车标志
在while（1）内做刹车处理，处理完后关闭软件刹车，使能PWM输出
================================================================================
测试环境
================================================================================
测试用板:
---------------------
EV-HC32F460-LQFP100-050-V1.1

辅助工具:
---------------------

辅助软件:
---------------------
无

================================================================================
使用步骤
================================================================================
1） 示波器查看PE9(J6-40)
2） PA0(J5-23)接SW2(J9-84)常高，DAC或PC03设置参考电压
3） 按下SW2（PA0为L）,下载并运行。
4） 可以看到PWM输出，释放SW2（PA00）变高电平，PWM输出关闭
5） SW2重新按下（PA00接地电平），延时之后PWM恢复输出。

================================================================================
注意
================================================================================
CMP输出高的时候，EMB刹车有效，当清除CMP刹车标志并退出中断，PWM会立刻恢复输出，
例程在CMP中断内设置软件刹车使能，然后主循环内停机处理完成后关闭软件刹车

================================================================================
